// API key is loaded from .env when you run the dev server:
//   npm install && npm start
// Then open http://localhost:3000
// Add MAPS_API_KEY=your_key to .env (enable Maps JavaScript API + Places API in Google Cloud).
